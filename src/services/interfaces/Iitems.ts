interface Iitem {
    id: number,
    brand: string,
    title: string,
    price: number[],
    imageUrl: string,
    volume: number[],
    gender: number,
    description: string
}

export default Iitem